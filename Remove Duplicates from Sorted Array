class Solution {
    public int removeDuplicates(int[] nums) {
        HashSet<Integer> s=new HashSet<Integer>();
        int i=0;
        for(int a:nums)
        {
           if(s.add(a)){
            nums[i]=a;
            i++;
           }
        }
        return i;
    }
}
