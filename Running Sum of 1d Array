class Solution {
    public int[] runningSum(int[] nums) {
        int sum=0;
        int[] s=new int[nums.length];
        for(int i=0;i<nums.length;i++){
            s[i]=sum+=nums[i];
        }
            return s;
    }
}
